// preprocessor
#include <iostream>
using namespace std;

// main start here ...
int main()
{
  cout << "> binary search tree is the variant of the binary tree :" << endl;
  cout << "> left node data -> is less than the parent node ->data" << endl;
  cout << "> right node data is greater than the parent node ->data" << endl;
  cout << "> the binary search tree is a binary tree in which each node has at most two children" << endl;
  cout << "⭐⭐ Application of bst ⭐⭐" << endl;
  cout<<endl;
  cout << "1. Avl tree / red black tree implementation using bst. " << endl;
  cout << "2. insertion , searching , deletion of take O(log n); " << endl;
  return 0;
}
