//preprocessor
#include<iostream>
using namespace std;


//main start here ...
int main(){
  cout<<"welcome to stack implementation in the cpp "<<endl;
  return 0;
}

// stack operation in the cpp 

// lifo => last in first out 
// implemented by using linked list and array 

// index start from the 0 ; 

// overflow  -> top >size;
// underflow -> top == -1;
// push  ->   push element on top of the stack and increment top index by 1 
// pop -> remove the element from the top and decrease the top index by 1 
